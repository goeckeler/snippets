package com.goeckeler.producer;

public class Simulation
{
   public void run(int producers, int consumers, int sizeOfQueue, int numbers) {
     // create a broker that is the coupling link between producers and consumers
     //
     
     // create at least one producer
     for(int p = 1; p <= Math.max(1, producers); ++p) {
       
     }
     
     // create at least one consumer
     for(int c = 1; c <= Math.max(1, consumers); ++c) {
       
     }
     
   }
}
